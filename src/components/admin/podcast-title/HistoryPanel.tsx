import { uiCopy } from '@/lib/podcast-title/uiCopy';
import type { EpisodeSummary } from '@/lib/podcast-title/adminApi';
export function HistoryPanel({ episodes, loading, error, onSelect }: { episodes: EpisodeSummary[]; loading: boolean; error: boolean; onSelect: (id: string) => void }) { return <section dir="rtl" className="rounded-xl border border-[var(--border)] bg-[var(--surface)] p-4"><h2 className="font-bold">{uiCopy.history}</h2>{loading && <p className="mt-3 text-sm text-[var(--muted)]">{uiCopy.analysisInProgress}</p>}{error && <p className="mt-3 text-sm text-red-400">{uiCopy.genericError}</p>}{!loading && !error && episodes.length === 0 && <p className="mt-3 text-sm text-[var(--muted)]">{uiCopy.noHistory}</p>}<ul className="mt-3 space-y-2">{episodes.map(episode => <li key={episode.episodeId}><button type="button" onClick={() => onSelect(episode.episodeId)} className="w-full rounded border border-[var(--border)] p-3 text-right"><span className="block font-medium" dir="auto" style={{ unicodeBidi: 'isolate' }}>{episode.recommendedTitle || episode.currentTitle || episode.episodeId}</span><span className="mt-1 flex gap-2 text-xs text-[var(--muted)]"><span>{episode.decision}</span><span>{episode.confidence}%</span></span></button></li>)}</ul></section>; }
